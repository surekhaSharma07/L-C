package com.intimetec.newsaggregation.menu;

/** A very small abstraction – every menu presenter just knows how to show itself. */
public interface MenuPresenter {
    void showMenu() throws Exception;
}
